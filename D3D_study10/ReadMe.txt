절대축 기준 회전
  - D3DXMatrixRotationX(&matRotation, GetTickCount() * 0.001f);
  - D3DXMatrixRotationY(&matRotation, GetTickCount() * 0.001f);
  - D3DXMatrixRotationZ(&matRotation, GetTickCount() * 0.001f);

컴퓨터의 틱 카운트를 Get! (단위 ms)
  - GetTickCount()